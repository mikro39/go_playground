package main

import (
	"fmt"
)

func main() {
	x := 5        // := is shorthand for var
	var y int = 3 //example of declared
	var sum int = x + y

	fmt.Println("This is a simple var operator doing math 5 + 3")
	fmt.Println(sum)
}
